export type FixMeLater = any;

export type SurveyForm = {
  title: string;
  subject: string;
  body: string;
  recipients: string;
};

export type PaymentForm = {
  name: string;
  email: string;
  phone: string;
  amt: string;
};
